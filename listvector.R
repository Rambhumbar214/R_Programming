#write an R program to create a list of element using vector matices and a function print the content of the lsit
l=list(
  c(1,2,2,5,7,12),
  month.abb,
  matrix(c(3,-8,1,-3),nrow=2),
  asin
)
print("content off the list")
print(l)